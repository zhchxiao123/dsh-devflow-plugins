/**
 * Client-namespace projection of this package's wire vocabulary. Client code
 * imports only the client namespace, so `./client` serves the browser half the
 * same single-source content `./types` serves host consumers.
 *
 * @module @zhchxiao123/dsh-devflow-web/client
 */

export type * from './types.ts'
